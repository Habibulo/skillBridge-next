export enum NotificationType {
	LIKE = 'LIKE',
	COMMENT = 'COMMENT',
}

export enum NotificationStatus {
	WAIT = 'WAIT',
	READ = 'READ',
}

export enum NotificationGroup {
	MEMBER = 'MEMBER',
	ARTICLE = 'ARTICLE',
	JOB_POST = 'JOB_POST',
}
